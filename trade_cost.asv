function [cost] = trade_cost(titan_dem, node1, node2)
% function [cost] = trade_cost(robot, titan_dem, node1, node2)

titan_radius = 2575e3; % m

dist = great_circle(titan_dem, titan_radius, node1, node2);

% cost is made of up
% energy
% time = dist/robot.max_speed
% risk (?)
% sortie distance (energy per distance)
cost = dist;
if titan_dem(node2(1), node2(2)) > 100
    cost = 10*cost;
end
% cost = dist/robot.max_speed;
